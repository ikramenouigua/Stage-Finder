<form method="post" action="{{ path('app_hello_delete', {'id': hello.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hello.id) }}">
    <button class="btn">Delete</button>
</form>
